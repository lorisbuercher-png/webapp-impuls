"use client";
import { useState } from "react";
import MitarbeiterLayout from "../../../components/MitarbeiterLayout";
export default function Profil(){const [edit,setEdit]=useState(false);const [saved,setSaved]=useState(false);const [data,setData]=useState({name:"Mehmet Yilmaz",email:"mehmet@email.ch",phone:"+41 79 555 12 34",address:"Musterstrasse 12, 8000 Zürich",iban:"CH93 0076 2011 6238 5295 7"});return <MitarbeiterLayout><div className="mx-auto max-w-4xl"><div className="flex justify-between"><div><h1 className="text-3xl font-black">Mein Profil</h1><p className="mt-2 text-gray-500">Persönliche Angaben kontrollieren und aktualisieren.</p></div><button onClick={()=>{if(edit)setSaved(true);setEdit(!edit)}} className="rounded-xl bg-[#6D5DF6] px-5 py-3 font-bold text-white">{edit?"Speichern":"Bearbeiten"}</button></div>{saved&&!edit&&<div className="mt-5 rounded-2xl bg-emerald-50 p-4 font-semibold text-emerald-700">✓ Änderungen gespeichert.</div>}<div className="mt-7 rounded-3xl border bg-white p-7"><div className="grid gap-5 md:grid-cols-2">{Object.entries(data).map(([key,value])=><label key={key} className="text-sm font-semibold capitalize text-gray-600">{key}<input disabled={!edit} value={value} onChange={e=>setData({...data,[key]:e.target.value})} className="mt-2 w-full rounded-xl border p-3 font-normal disabled:bg-gray-50"/></label>)}</div></div></div></MitarbeiterLayout>}

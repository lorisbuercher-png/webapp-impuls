import Link from 'next/link'; import Nav from '@/components/Nav'; import { jobs } from '@/lib/data';
export default function Jobs(){return <><Nav/><main className="mx-auto max-w-6xl px-4 py-12"><h1 className="mb-8 text-4xl font-bold">Jobbörse</h1><div className="grid gap-6 md:grid-cols-3">{jobs.map(job => <Link href={`/jobs/${job.id}`} key={job.id} className="card transition hover:scale-[1.02]"><h2 className="text-xl font-bold">{job.title}</h2><p className="mt-2 text-white/70">{job.location} · {job.workload}</p><p className="mt-4 text-secondary">{job.salary}</p></Link>)}</div></main></>}

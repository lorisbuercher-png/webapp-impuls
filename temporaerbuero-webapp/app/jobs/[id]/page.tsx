import Link from 'next/link'; import Nav from '@/components/Nav'; import { jobs } from '@/lib/data';
export default function JobDetails({ params }: { params: { id: string } }){ const job = jobs.find(j => j.id === params.id) || jobs[0]; return <><Nav/><main className="mx-auto max-w-3xl px-4 py-12"><div className="card"><p className="text-secondary">{job.location} · {job.workload}</p><h1 className="mt-2 text-4xl font-bold">{job.title}</h1><p className="mt-4 text-white/70">{job.description}</p><p className="mt-6 text-xl font-semibold">{job.salary}</p><Link className="btn-primary mt-8 inline-block" href={`/bewerbung?job=${job.id}`}>Jetzt bewerben</Link></div></main></>}
